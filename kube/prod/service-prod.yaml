apiVersion: v1
kind: Service
metadata:
  labels:
    app: qscore-be-srv
  name: qscore-be-srv
  namespace: qscore
spec:
  ports:
    - port: 80
      protocol: TCP
      targetPort: 8000
  selector:
    app: qscore-be
